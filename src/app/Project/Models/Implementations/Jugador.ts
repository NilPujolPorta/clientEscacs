export class Jugador {
  nom: string;
  color: string;
  taulell: string;
  id: string;
  constructor(
    nom: string,
    color: string,
    taulell: string,
    id: string
  ) {
    this.nom = nom;
    this.color = color;
    this.taulell = taulell;
    this.id = id;

  }
}
